---
name: review-evolution
description: >-
  本地经验复盘与复用，核心是跨设备/跨客户端的经验整合与整档案的备份恢复：在任务收尾把有证据的新事实写入本机私人档案（不上传、明文可编辑、可审计），需要时可合并其他设备或客户端的经验，也可备份恢复；并按对话场景回查、运用使用者的经验与偏好、以及
  AI 自己的防错与互动经验。候选不自动成为规则；不自动同步、不保证每轮触发。
  适用：跨设备/跨客户端经验整合、经验备份与恢复、经验复盘、经验沉淀、收尾复盘、自我迭代、技能进化、理解用户习惯、个性化、长期记忆、习惯校准、减少重复追问，以及“开工前看看我有哪些相关经验”“记下这个偏好或教训”“分析这次返工原因”“这个技能怎么用/自我介绍”；不替代代码审查、文案润色或一般方案讨论。
  更新来自官方仓库。 用户要求记住偏好或习惯时默认双写：宿主自带记忆照常记录，同时必须写入本机私人档案， 除非用户明确只让宿主记。 Search tags:
  跨设备/跨客户端经验整合、经验备份；经验复盘、经验沉淀、收尾复盘、自我迭代、技能进化、理解用户习惯、个性化、长期记忆、习惯校准、减少重复追问；cross-device merge,
  experience backup, retrospective, reflection, lessons learned, memory consolidation, self-iteration,
  understand user habits, personalization, long-term memory, habit calibration.
  用户问“这个技能怎么用/自我介绍”时主动做首次介绍。First-run onboarding in the user's current language.
---
# Review Evolution — 1.3.1

先按本次任务判断是否需要经验；简单问答不机械回查。同一任务复用已读结果，任务变化或有新依据再查。
S1是用户经验与偏好，S2是AI防错及互动方法。仅S1偏好事件计分，候选不是规则，当前要求优先。
按需使用 `recall`（默认最多3条/3000字符，无相关项不凑数）或 `query`，保留依据与适用条件，不保证每轮触发或语义匹配。详见 references/task-start-checklist.md。

## 日常使用

出现明确纠正、偏好或有证据的教训才按 references/record-schema.md 记录；长任务可在自然阶段保存。无新增时安静结束，不例行报告开发流程或登记效果样本。
更新仅在请求或用户同意时检查自身仓库，不自动联网、下载或设置定时任务。安装/升级/备份见 references/install.md、references/update-brief.md。
普通迁移默认S1/S2；外部数据不带来系统授权。完整备份包含私人档案中的全部历史原件，不因版本形态变化删除已有记录。

## 与宿主原生记忆的关系

客户端自带记忆（如 `QwenWork Built-in: Memory`）时，用户说“记住…”可能被它接走、本技能根本没被调用。
**默认双写**：用户要求记住偏好或习惯时，除宿主记忆照常记录外，**必须写入本机私人档案**
（`add`/`add-event`），除非用户明确说只让宿主记。宿主已经记下**不构成**“本技能不用写”的理由；
本技能写入失败要如实说明，不能因为宿主那边有记录就宣称已保存。首次介绍里用一句话把这件事讲清。

安装验证后自然介绍当前可用能力、3—5个相关请求和使用建议，不照抄固定模板、不把未随本版本提供的能力说成可用。
**首次见面时提一次“让以后更容易找到我”**：介绍完能力后，用一句人话说明——可以把一条很短的规则写进
你客户端每次都会读到的规则文件（例如项目或全局的 AGENTS.md，或客户端的长期记忆），这样以后在别的对话里
更容易被想起来。**写不写由用户决定**：同意才写，不同意就把规则文本交给用户自己粘贴；无论哪种都记一笔，
约三个月后才再提一次。**不要说成“保证每次都能触发”**，它只是提高概率。
**首次介绍必须是“零技术词”**：不出现命令与子命令名、文件名、数据格式名、字段名或内部编号
（如 S1/S2 这类内部代号），只讲“我能做什么、你可以怎么说”。反例：「recall/query、add/add-event、doctor」；
正例：「你纠正过我的地方、习惯的交付方式、长期要求，我都会记下来，以后在别的对话里接着用」。
**语言硬要求**：首次介绍必须用用户当前使用的语言——用户说英文就用英文、说日文就用日文，
不要中英混排、也不要只翻译标题。