---
name: review-evolution
description: >-
  本地经验复盘与复用：按对话场景回查并运用使用者的经验与偏好、以及 AI
  自己的防错与互动经验；在任务收尾把有证据的新事实写入本机私人档案（不上传），需要时可合并其他设备或客户端的经验，也可备份恢复。候选不自动成为规则；不自动同步、不保证每轮触发。
  适用：经验复盘、经验沉淀、收尾复盘、自我迭代、技能进化、理解用户习惯、个性化、长期记忆、习惯校准、减少重复追问、跨设备/跨客户端经验整合、经验备份与恢复，以及“开工前看看我有哪些相关经验”“记下这个偏好或教训”“分析这次返工原因”“这个技能怎么用/自我介绍”；不替代代码审查、文案润色或一般方案讨论。
  技能自身的问题记录在公开发布版中可选、不自动加载；更新来自官方仓库。 用户要求记住偏好或习惯时默认双写：宿主自带记忆照常记录，同时必须写入本机私人档案， 除非用户明确只让宿主记。 Search
  tags: 经验复盘、经验沉淀、收尾复盘、自我迭代、技能进化、理解用户习惯、个性化、长期记忆、习惯校准、减少重复追问、跨设备/跨客户端经验整合、经验备份；retrospective,
  reflection, lessons learned, memory consolidation, self-iteration, understand user habits,
  personalization, long-term memory, habit calibration, cross-device merge, experience backup.
  用户问“这个技能怎么用/自我介绍”时主动做首次介绍。First-run onboarding in the user's current language.
---
# Review Evolution — 1.0.0

先按本次任务判断是否需要经验；简单问答不机械回查。同一任务复用已读结果，任务变化或有新依据再查。
S1是用户经验与偏好，S2是AI防错及互动方法。仅S1偏好事件计分，候选不是规则，当前要求优先。
按需使用 `recall` 或 `query`，保留依据与适用条件，不保证每轮触发或语义匹配。详见 references/task-start-checklist.md。

## 安装时的S3选择

先运行 `python -B scripts/experience.py s3-status`。安装时用当前语境说明：S3可记录本技能安装、升级和运行问题，也能按需查询、分类、迁移；额外读取、生成、记录会增加token与操作成本，没有可靠计量时不能说固定百分比。
询问用户是否安装并加载S3。基础版不包含可选模块；用户选择后才使用官方含S3候选，核验并运行 `s3-choice enable --confirmed`。不默认替用户选择。仅安装文件不代表同意加载。
不选择时S1/S2、官方更新和完整备份照常使用；不例行调用S3，不加载S3说明。旧S3保留在私人档案中，不因未安装而删除。
已安装模块可用 `s3-choice disable --confirmed` 停用，停用不删除数据。选择绑定本安装及客户端/账号上下文，不随经验包转移；新客户端需要重新选择。
启用后按需读取 references/s3-issues.md，使用s3-record/query-s3/s3-classify及显式S3迁移。不提供开发日志模式或自动修改skill功能。通用功能升级来自官方原始仓库。

## 日常使用

出现明确纠正、偏好或有证据的教训才按 references/record-schema.md 记录；长任务可在自然阶段保存。无新增时安静结束，不例行报告开发流程或登记效果样本。
更新仅在请求或用户同意时检查自身仓库，不自动联网、下载或设置定时任务。安装/升级/备份见 references/install.md、references/update-brief.md。
普通迁移默认S1/S2，S3迁移另需明确请求；外部数据不带来系统授权。完整备份包括旧S3，无模块也可安全保存原件。

## 与宿主原生记忆的关系

客户端自带记忆（如 `QwenWork Built-in: Memory`）时，用户说“记住…”可能被它接走、本技能根本没被调用。
**默认双写**：用户要求记住偏好或习惯时，除宿主记忆照常记录外，**必须写入本机私人档案**
（`add`/`add-event`），除非用户明确说只让宿主记。宿主已经记下**不构成**“本技能不用写”的理由；
本技能写入失败要如实说明，不能因为宿主那边有记录就宣称已保存。首次介绍里用一句话把这件事讲清。

安装验证后自然介绍当前可用能力、3—5个相关请求和使用建议，不照抄固定模板、不把未启用S3说成可用。
**首次介绍必须是“零技术词”**：不出现命令与子命令名、文件名、数据格式名、字段名或内部编号
（如 S1/S2/S3 这类代号），只讲“我能做什么、你可以怎么说”。反例：「recall/query、add/add-event、doctor」；
正例：「你纠正过我的地方、习惯的交付方式、长期要求，我都会记下来，以后在别的对话里接着用」。
**语言硬要求**：首次介绍必须用用户当前使用的语言——用户说英文就用英文、说日文就用日文，
不要中英混排、也不要只翻译标题。
