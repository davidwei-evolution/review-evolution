# 更新说明

## 相对上一个正式发布版本
`1.0.0` → `1.3.1`

### 新增
- 发布形态简化为单一技能目录：解压到客户端技能目录即可使用，不再需要任何安装选择步骤。

### 变化
- 相关任务默认最多提供3条经验；简单问题不会例行查阅经验，也不再在每次对话结束时登记效果样本。
- 安装材料改为只保留当前使用所需的说明、限制和升级指引，不再附带早期版本沿革或维护过程记录。
- 升级兼容范围明确为从 1.0.0 起：更早版本仍可直接升级、无需手工替换，但不再作为后续版本的保证范围。
- 合并其他设备或客户端的经验时，不再自动用对方的记录改变你本机已有经验的状态：无争议的内容照常一次导入，两端状态不一致的会先被扣下并集中列出，由你逐项决定是否接受。
- 整理重复经验时，给一条经验写“停用”不再需要补齐原因/预防/信号等字段，直接写明停用原因即可。
- 合并时列出的差异清单统一了字段：每条都给出主键、原因、被扣下的文件、以及本机与对方的原文/状态，便于直接阅读或由脚本处理。

### 修复
- 改进了部分客户端在技能目录写入附加文件时的诊断和测试指引，并避免未初始化档案的失败写入留下阻碍后续使用的残留状态。
- 修正了合并时遇到“同一条经验在两端内容不一样”就整体中止的问题：现在无争议的内容照常一次导入完，内容不一致的那几条先保留你本机版本、并和其他差异一起集中列出，由你决定是否采用对方版本。
- 升级时如果已装文件被客户端改写导致校验不过，以前只能报错停下；现在可以在升级命令上显式加一个修复参数，用新版安装包里的官方同名文件覆盖这几处并重新校验，不必再去找旧版本的安装包。